# ====================================================================
# Docker Compose 설정 (현재 사용 안 함 - 외부 개발 DB 사용)
# ====================================================================
#
# 이 프로젝트는 외부 개발 DB를 사용합니다.
# .env 파일에서 DB 연결 정보를 설정하세요.
#
# 로컬 Docker DB가 필요한 경우 아래 주석을 해제하여 사용할 수 있습니다.
# ====================================================================

# services:
#   mariadb:
#     image: mariadb:11.2
#     container_name: ku-mariadb
#     restart: unless-stopped
#     ports:
#       - '3306:3306'
#     environment:
#       MYSQL_ROOT_PASSWORD: password
#       MYSQL_DATABASE: ku_wave_plat
#       MYSQL_USER: ku_user
#       MYSQL_PASSWORD: password
#       MARIADB_CHARACTER_SET_SERVER: utf8mb4
#       MARIADB_COLLATE_SERVER: utf8mb4_unicode_ci
#     volumes:
#       - mariadb-data:/var/lib/mysql
#     networks:
#       - ku_default
#     healthcheck:
#       test: ['CMD', 'healthcheck.sh', '--connect', '--innodb_initialized']
#       interval: 10s
#       timeout: 5s
#       retries: 5
#       start_period: 30s
#     deploy:
#       resources:
#         limits:
#           cpus: '2'
#           memory: 1G
#         reservations:
#           cpus: '1'
#           memory: 512M
#     stop_grace_period: 30s
#     stop_signal: SIGTERM
#     command:
#       - --character-set-server=utf8mb4
#       - --collation-server=utf8mb4_unicode_ci
#       - --max_connections=200
#       - --innodb_buffer_pool_size=512M
#
# networks:
#   ku_default:
#     name: ku_default
#     driver: bridge
#
# volumes:
#   mariadb-data:
